{
  "name": "soundcloud-to-text",
  "version": "1.0.0",
  "description": "Export SoundCloud profile data as a text file, deployable on Vercel.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --check api/export.js"
  },
  "dependencies": {
    "axios": "^1.14.0"
  },
  "engines": {
    "node": ">=20"
  }
}
